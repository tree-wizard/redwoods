//package version defines the buildversions and its time
package version

var (
	// BuildTime the time the image was built
	BuildTime = "unset"
	// Commit is the name of the commit the version wasb build from
	Commit = "unset"
	// Release is the release version of our program
	Release = "unset"
)
